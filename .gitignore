# Секреты — никогда не коммитим
.env
*.env
!.env.example

# Python
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# IDE
.idea/
.vscode/

# Airflow
airflow.db
airflow.cfg
logs/

# Офлайн-бандлы: собранные архивы — артефакты сборки
voila-offline/voila-offline.tar.gz
# .whl не версионируем — 22 МБ на каждый клон. Кладутся локально перед install.sh
# (своя копия дистрибутива или `pip download voila -d voila-offline/wheels`).
voila-offline/wheels/

# Сгенерированные общие виды конфигов (источник — фрагменты config.d/, SpTableName.d/, ...)
# Рантайм читает фрагменты; эти файлы — только для просмотра, их пересобирает tools/regen_config.py
etlFolder/config.json
etlFolder/SpTableName.json
etlFolder/SpOnce.json
