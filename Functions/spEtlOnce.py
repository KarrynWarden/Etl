"""Разовый перенос справочника.

Логика совпадает с регулярным справочником (полная перезаливка: DELETE FROM
ведомой + пакетный INSERT всех строк ведущей, с логированием) — поэтому здесь
переиспользуется единый `SpEtl` из `Functions.spEtlNew`, а не отдельная копия.

Параллелизм разового переноса обеспечивает сам даг `dags/SpOnce.py`: он создаёт
по задаче на таблицу, и Airflow гоняет их параллельно. Раньше здесь была
собственная последовательная реализация — она убрана как дублирующая.
"""
from Functions.spEtlNew import SpEtl  # noqa: F401
