import { Alert, Card, Collapse, Space, Typography } from 'antd'

// Справка. Не пересказ интерфейса — кнопки и так подписаны, — а ответы на
// вопросы, которые возникают у экрана и которые дороже всего искать: почему
// линия молчит без единой ошибки, почему страница пустая, что значит «нет
// маршрута». Каждый из этих случаев уже стоил разбирательств.
const { Paragraph, Text, Title } = Typography

export default function HelpPage() {
  return (
    <Space direction="vertical" size="middle" style={{ display: 'flex' }}>
      <Card size="small" title="Что здесь есть">
        <Paragraph>
          <Text strong>Сложный ETL</Text> — линии со своим дагом и структурами.
          Перенос пишет строку в ведомую <b>по позициям структур</b>, а не по
          именам: пара с разными именами (<Text code>GROUPPCODE</Text> /{' '}
          <Text code>groupcode</Text>) — это норма, а сдвиг на строку — беда.
          Поэтому на вкладке «Колонки» позиции показаны намеренно.
        </Paragraph>
        <Paragraph>
          <Text strong>Справочники</Text> обновляются <b>по сигналу</b>, а не по
          расписанию: аудитный триггер ведущей ставит{' '}
          <Text code>isokaudit = 0</Text> в <Text code>etl_jobs</Text>, и даг
          забирает все линии с такой <Text code>dependence</Text>. Нет триггера —
          справочник не обновится никогда и ошибки при этом не выдаст.
        </Paragraph>
        <Paragraph>
          <Text strong>Разовый перенос</Text> запускают руками под задачу.
          Сигнал ему не нужен вовсе.
        </Paragraph>
        <Paragraph style={{ marginBottom: 0 }}>
          <Text strong>Версии и прод</Text> — выкладка и откат. Прод не «хвост»
          теста: каждая выкладка это коммит поверх предыдущего прода с деревом
          теста, помеченный тегом <Text code>prod-ГГГГММДД-ЧЧММ</Text>.
        </Paragraph>
      </Card>

      <Alert
        type="info"
        showIcon
        message="На диск — только по кнопке"
        description={
          <>
            Ни один переключатель в конструкторе не пишет файлы сам. Всё, что вы
            меняете, живёт в форме до «Предпросмотра», а на диск попадает
            кнопкой «Записать» — и до неё видно построчный дифф каждого файла.
            Единственное исключение — «Выложить на прод»: её последствие в
            предпросмотре не покажешь, поэтому она просит слово-подтверждение.
          </>
        }
      />

      <Card size="small" title="Когда что-то идёт не так">
        <Collapse
          items={[
            {
              key: 'route',
              label: 'Кнопка отвечает «нет маршрута»',
              children: (
                <>
                  <Paragraph>
                    Это <b>сервис старше страницы</b>, а не поломка интерфейса.
                    Страницу браузер берёт с диска, и после обновления рабочей
                    копии она сразу новая. А python остался тем, что запустился:
                    маршрутов из нового кода в нём нет.
                  </Paragraph>
                  <pre style={{ margin: 0 }}>
                    sudo systemctl restart etl-dagbuilder-api
                  </pre>
                </>
              ),
            },
            {
              key: 'blank',
              label: 'Страница открылась пустой',
              children: (
                <Paragraph style={{ marginBottom: 0 }}>
                  Почти всегда это разъехавшиеся <Text code>index.html</Text> и{' '}
                  <Text code>assets/</Text>: имя файла сборки содержит хэш, и
                  если <Text code>dist</Text> дотянули не целиком, страница
                  ссылается на файл, которого рядом нет. Ошибки при этом не
                  видно никакой. Сервис проверяет это при старте и пишет в
                  журнал — <Text code>journalctl -u etl-dagbuilder-api</Text>.
                </Paragraph>
              ),
            },
            {
              key: 'silence',
              label: 'Линия молчит: ни ошибок, ни строк',
              children: (
                <>
                  <Paragraph>
                    Самая дорогая поломка в этом проекте, потому что выглядит
                    как исправная работа. Обычные причины:
                  </Paragraph>
                  <ul style={{ marginTop: 0 }}>
                    <li>
                      <b>Имя линии разошлось.</b>{' '}
                      <Text code>tableNameEtlJobs</Text> живёт и в файлах, и в
                      базе: триггер ведущей <b>пишет</b> его в{' '}
                      <Text code>etl_log_iud_row.tablename</Text>, а перенос и
                      аудит <b>ищут</b> по нему в{' '}
                      <Text code>etl_jobs.tablename</Text>. Сравнение
                      дословное, с учётом регистра. Поэтому переименование
                      линии — операция с планом и SQL, а не поле в форме.
                    </li>
                    <li>
                      <b>У справочника нет аудитного триггера.</b> Без{' '}
                      <Text code>isokaudit = 0</Text> дагу нечего забирать. DDL
                      берётся на вкладке «Действия» линии.
                    </li>
                    <li>
                      <b>Триггера нет на ведущей сложной линии.</b> Режим{' '}
                      <Text code>iud</Text> питается журналом{' '}
                      <Text code>etl_log_iud_row</Text>; пустой журнал — пустой
                      перенос. Вкладка «Триггеры» показывает, кому он нужен, и
                      сверяет с тем, что реально стоит в БД.
                    </li>
                  </ul>
                </>
              ),
            },
            {
              key: 'flash',
              label: 'Перенос правок через флешку',
              children: (
                <>
                  <Paragraph>
                    Копировать папки с заменой нельзя: копирование добавляет и
                    обновляет, но <b>никогда не удаляет</b>. Удалённая линия так
                    не удалится — её фрагмент останется лежать и вернётся в
                    работу молча.
                  </Paragraph>
                  <pre style={{ margin: 0 }}>
{`cd <папка репозитория>
python3 tools/sync_zip.py Etl-main.zip --base          # первый раз
python3 tools/sync_zip.py Etl-main.zip --update-self   # обновить сам скрипт
python3 tools/sync_zip.py Etl-main.zip                 # перенос`}
                  </pre>
                  <Paragraph style={{ marginTop: 8, marginBottom: 0 }}>
                    Путь набирать не нужно — архив ищется на подключённых
                    носителях по имени, и <Text code>unzip</Text> так не умеет:
                    отсюда <Text code>--update-self</Text>, которым скрипт
                    достаёт из архива свою же новую версию. Ваши структуры и
                    запросы слияние не трогает: файла нет ни в одном снимке —
                    значит он ваш. Больше того: запросы, структуры и конфиги
                    линий пишет конструктор на этой машине, поэтому мои правки
                    в них не применяются вовсе — только показываются. Оборотная
                    сторона того же правила — мусор от переименований; его
                    слияние называет списком, а убирает только по указанному
                    пути: <Text code>--clean tools/webui/src</Text>.
                  </Paragraph>
                </>
              ),
            },
          ]}
        />
      </Card>

      <Card size="small" title="Где читать дальше">
        <Paragraph style={{ marginBottom: 0 }}>
          <Text code>README.md</Text> — устройство переносов и режимов;{' '}
          <Text code>tools/webui/README.md</Text> — этот интерфейс;{' '}
          <Text code>deploy/README-prod.md</Text> — тест и прод;{' '}
          <Text code>local/README.md</Text> — работа с dev-ПК и перенос через
          флешку.
        </Paragraph>
      </Card>

      <Title level={5} type="secondary" style={{ fontWeight: 400 }}>
        Не нашли ответа — посмотрите в журнал сервиса: он пишет туда причину
        отказа целиком, а в интерфейс отдаёт только текст.
      </Title>
    </Space>
  )
}
