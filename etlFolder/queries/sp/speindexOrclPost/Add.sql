-- Разовый перенос SPEINDEX: вставка в ведомую speindex на PostgreSQL.
--
-- Пакет строк грузит psycopg2.extras.execute_values (Functions/spEtlNew.py,
-- ветка dbSlave='Post'): он сам разворачивает плейсхолдер пакета в
-- (...),(...),(...) на все строки пакета. Oracle-биндов (двоеточие с номером)
-- здесь быть не может — на них execute_values падает с "the query doesn't
-- contain any placeholder", ровно это и случилось на проде, когда оба
-- направления делили один Add.sql.
--
-- ВАЖНО: плейсхолдер в файле должен быть РОВНО ОДИН, и в комментариях его
-- писать нельзя — комментарии тоже часть запроса, execute_values считает
-- вхождения по всему тексту и на втором падает.
--
-- Обратное направление (SPEINDEXPostOrcl, PG -> Oracle) живёт в
-- queries/sp/SPEINDEX/ и остаётся с Oracle-биндами — файлы намеренно разные.
--
-- filedname в перенос не входит: колонка есть в таблице Oracle, а на стороне
-- Postgres ею не пользуются — обратное направление подставляет в неё пустую
-- строку вместо значения из PG. Если колонка на PG всё же нужна с данными,
-- добавить её в оба файла пары (сюда и в Select.sql), сохранив порядок.
--
-- Порядок колонок обязан совпадать с Select.sql: значения подставляются
-- позиционно, по именам ничего не сопоставляется.
INSERT INTO speindex (code, name, eindexform, typeindex, intonly, ei, period,
                      intotemo, kosgu, fsum, formula, numbord, signext)
VALUES %s
