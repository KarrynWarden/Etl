--------------------------------------------------------------------------------
-- Postgres: что остаётся ненужным после перевода продления ЭИНДЕКСов на даг
-- A61ProceduresScanLogProlong (журнал заявок живёт только на Oracle).
--
-- НИЧЕГО ИЗ ЭТОГО НЕ ОБЯЗАТЕЛЬНО и ничего не выполняется автоматически: файл —
-- памятка, что можно убрать, когда новый даг отработает и в этом не останется
-- сомнений. Выполнять руками, по одному, глядя на результат.
--
-- Что продолжает использоваться и трогать НЕЛЬЗЯ:
--   * pck_eindexmo.ProlongDestrEindex — её теперь зовёт даг;
--   * public.ias_func_errlog          — туда даг пишет ошибки (idfunc = 286).
--------------------------------------------------------------------------------

-- 1. Процедура-обход журнала ---------------------------------------------------
-- pck_eindexmo.scanlogprolong читала копию log_eindprolong на Postgres. Копия
-- больше не наполняется, значит процедура — no-op. Прежде чем удалять, стоит
-- убедиться, что её не зовёт кто-то ещё (джоб, другое приложение):
--   SELECT * FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
--    WHERE prosrc ILIKE '%scanlogprolong%' AND p.proname <> 'scanlogprolong';
--
-- DROP PROCEDURE pck_eindexmo.scanlogprolong();


-- 2. Копия журнала -------------------------------------------------------------
-- Данные в ней замерли на моменте отключения переноса. Если по ней ничего не
-- строится (отчёты, вьюхи), таблицу можно удалить:
--   SELECT count(*), max(timeoper) FROM log_eindprolong;
--
-- DROP TABLE log_eindprolong;
