-- Записи одной группы из ведущего источника — то же, что берёт аудит.
--
-- Здесь был LEFT JOIN etl_jobs по (tablename, период). Он не делал НИЧЕГО:
-- в списке полей только колонки p, из e не выбиралось ни одной. Зато стоил
-- дорого — на iperson выборка группы шла 43 с против 3 с у точно такого же
-- запроса аудита без джойна. И был опасен: две строки etl_jobs, попадающие в
-- одну группу (например, период 1900-01-01 и NULL-группа — они совпадают под
-- COALESCE), задвоили бы КАЖДУЮ строку источника, а перезаливка залила бы
-- дубли в ведомую.
SELECT {fields_str}
FROM ( {select_sql} ) p
WHERE {period_cond}
