-- Продление срока действия ЭИНДЕКСа на Postgres по параметрам из журнала Oracle.
--
-- Пятый параметр процедура ждёт строкой вида 01.03.2026 (dd.mm.yyyy): в прежней
-- версии её готовил to_char(rec.destrdate,'dd.mm.yyyy'), здесь формат даёт
-- python (см. _asDateString в даге).
--
-- Явные ::integer нужны, чтобы при NULL в ключе не было неоднозначности
-- перегрузок: нетипизированный NULL Postgres сопоставить с сигнатурой не может.
--
-- ВАЖНО: в этом файле не должно быть знака "процент" вне плейсхолдеров —
-- psycopg2 подставляет параметры обычным форматированием строки и споткнётся
-- о него даже в комментарии.
CALL pck_eindexmo.ProlongDestrEindex(%s::integer, %s::integer, %s::integer,
                                     %s::integer, %s)
